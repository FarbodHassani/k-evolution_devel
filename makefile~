COMPILER     := mpic++
INCLUDE      := -I/home/farbod/Packages/packages/LATfield2 -I/home/farbod/Packages/packages/hdf5-1.8.17/hdf5/include -L/home/farbod/Packages/packages/hdf5-1.8.17/hdf5/lib -I./class_noppf/include
LIB          := -L./class_noppf -lfftw3 -lm -lhdf5 -lgsl -lgslcblas -lclass -fopenmp

# target and source
EXEC         := gevolution
SOURCE       := main.cpp
HEADERS      := $(wildcard *.hpp)

# mandatory compiler settings (LATfield2)
DLATFIELD2   := -DFFT3D -DHDF5

# optional compiler settings (LATfield2)
#DLATFIELD2   += -DH5_HAVE_PARALLEL -DH5_HAVE_PIXIE
#DLATFIELD2   += -DEXTERNAL_IO

# optional compiler settings (gevolution)
DGEVOLUTION  := -DPHINONLINEAR
DGEVOLUTION  += -DBENCHMARK
DGEVOLUTION  += -DEXACT_OUTPUT_REDSHIFTS
#DGEVOLUTION  += -DCHECK_B
DGEVOLUTION  += -DHAVE_CLASS # requires OPT -fopenmp and LIB -lclass

# further compiler options
OPT          := -O3 -std=c++11 -fopenmp

$(EXEC): $(SOURCE) $(HEADERS) makefile
	$(COMPILER) $< -o $@ $(OPT) $(DLATFIELD2) $(DGEVOLUTION) $(INCLUDE) $(LIB)

